# @znk-sk-tools/build

Shared build script for Vue 3 projects.

## Install

```bash
pnpm add -D @znk-sk-tools/build
```

## Usage

```bash
npx sk-build
```

Or in `package.json`:

```json
{
    "scripts": {
        "build": "sk-build"
    }
}
```
